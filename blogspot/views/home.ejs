<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p>
        <%= homecontent %>
    </p>
    <% posts.forEach((post) =>{ %>

        <h1>
            <%= post.title %>
        </h1>

        <% post.content.substring(0, 101) %> 
        <% const link = post.tilte %>
        <p><%= post.content.substring(0, 101) + "..." %> <a href="/post/<%=post.title%>" class="text-muted fw-bold">Read more</a></p>

    <% }) %>

<%- include("partials/footer"); -%>

